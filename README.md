<p align="center">
  <a href="https://laravel.com" target="_blank">
    <img src="https://raw.githubusercontent.com/laravel/art/master/logo-lockup/5%20SVG/2%20CMYK/1%20Full%20Color/laravel-logolockup-cmyk-red.svg" width="400" alt="Laravel Logo">
  </a>
</p>

---

## 🧩 Задача

Вам необходимо **стянуть все данные по описанным эндпоинтам** и сохранить их в базу данных.

Используется **Laravel** и **MySQL**.

**Требования доработки приложения:**

- Развернуть приложение с помощью Docker Compose с сервисами `php` и `mysql`.
- Использовать нестандартный порт MySQL (не 3306).
- Организовать ежедневное обновление данных дважды в день.
- Предусмотреть обработку ошибок API типа `Too many requests` и повторные попытки.
- Сделать вывод отладочной информации в консоль.
- Организовать структуру БД: компании, аккаунты, токены, API сервисы, типы токенов.
- Предусмотреть возможность добавления через консоль: компаний, аккаунтов, токенов, API сервисов, типов токенов.
- В таблицы данных добавить поле `account_id` и предотвратить затирание данных между аккаунтами.
- Получать только свежие данные по полю `date`.

---

## 🗄️ Данные БД

| Параметр | Значение |
|-----------|-----------|
| **Хост** | `10.0.0.110` |
| **База данных** | `a1183197_api` |
| **Пользователь** | `a1183197_api` |
| **Пароль** | `tPVovSLm` |

### Таблицы:

- `stocks` — остатки
- `incomes` — поступления
- `sales` — продажи
- `orders` — заказы
- `companies` — компании
- `accounts` — аккаунты
- `api_services` — API сервисы
- `token_types` — типы токенов
- `api_service_token_types` — связь сервисов и типов токенов
- `account_tokens` — токены аккаунтов

---

## ⚙️ Установка и запуск

### 1. Запуск контейнеров

```bash
docker-compose up -d --build
```

### 2. Установка зависимостей и генерация ключа приложения

```bash
docker-compose exec app composer install
docker-compose exec app php artisan key:generate
```

### 3. Выполнение миграций

```bash
docker-compose exec app php artisan migrate
```

### 4. Воркеры и планировщик

Воркеры и планировщик запускаются автоматически через сервисы `worker` и `scheduler`.

---

## 🚀 Импорт данных

Импорт выполняется через artisan-команду:

```bash
php artisan app:import-api-data --type=<тип> [--dateFrom=<дата>] [--dateTo=<дата>]
```

#### Доступные типы:

- `stocks` — остатки (только текущая дата)
- `incomes` — поступления
- `sales` — продажи
- `orders` — заказы

#### Примеры:

Импорт продаж за период:

```bash
php artisan app:import-api-data --type=sales --dateFrom=2025-10-01 --dateTo=2025-10-22
```

Импорт остатков:

```bash
php artisan app:import-api-data --type=stocks
```

---

## 🧪 Тестирование и добавление данных

### Добавляем компании

```bash
php artisan app:add-company "Wildberries"
php artisan app:add-company "Ozon"
```

### Добавляем аккаунты

```bash
php artisan app:add-account 1 "WB Account 1"
php artisan app:add-account 2 "Ozon Account 1"
```

### Добавляем API сервисы

```bash
php artisan app:add-api-service wildberries "Wildberries API"
php artisan app:add-api-service yandex "Yandex API"
```

### Добавляем типы токенов

```bash
php artisan app:add-token-type bearer "Bearer Token"
php artisan app:add-token-type api-key "API Key"
```

### Связываем токен с типом

```bash
php artisan app:bind-token-type 1 1
```

### Добавляем токены для аккаунтов

```bash
php artisan app:add-account-token 1 1 1 "E6kUTYrYwZq2tN4QEtyzsbEBk3ie"
```

### Импортируем данные

```bash
php artisan app:import-api-data --type=stocks
php artisan app:import-api-data --type=orders
php artisan app:import-api-data --type=sales --dateFrom=2025-10-01 --dateTo=2025-10-22
```

### Проверка

- Таблицы `stocks`, `orders`, `sales`, `incomes` должны содержать свежие записи по полю `date`.
- Для `stocks` дата запроса всегда текущая (today).
- Для `orders`, `sales`, `incomes` учитываются только свежие данные.
- Поддерживается несколько аккаунтов, предотвращается затирание данных между ними.
- Любые ошибки API обрабатываются с повторными попытками, информация выводится в консоль.

---
